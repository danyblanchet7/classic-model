&joboptions

! Model grid description
    projectedGrid = .false. ! True if you have a projected lon lat grid, false if not. Projected grids
                            ! can only have regions referenced by the indexes, not coordinates, when 
                            ! running a sub-region
! Run length:
    ! Set the run start and end years. If you intend to cycle the meteorology, this will happen automatically based upon the years 
    ! specified for actualMetStartYear and actualMetEndYear below. If it is intended to be a transient (e.g. historical or future)
    ! simulation, runStartYear and runEndYear should be years the same as, or within, the meteorological forcing files
    runStartYear = 1750,  ! First year of the simulation.
    runEndYear = 2015,    ! Last year of the simulation

! Meteorological options:

    actualMetStartYear = 2016, ! Year of meteorology to start from, either in a transient simulation or cycling over in a spinup run.
    actualMetEndYear = 2024,   ! Last year of meteorology to be used in run, either in a transient simulation or cycling over in a spinup run.

    metOrder = 'sequential',  ! Options are 'sequential' - this runs through the meteorology in year-order, or 'random' - this 
                              ! takes the years between actualMetStartYear and actualMetEndYear and puts them in random order
                              ! that will change between loops of the meteorology (intended for spinups). When set to 'random', 
                              ! it will reproduce the same year order each run given the same starting conditions. NOTE! 
                              ! 'random' does NOT work with leap = true. 

    leap = .true.         ! True if your meteorological forcing includes leap years. Note: If you intend to turn on 
                             ! fire and leap is true, you need to ensure your lightning file (LGHTFile) has leap days.
                             ! NOTE: metOrder must be 'sequential' if leap = true!

    allLocalTime = .true.,  !< If your gridded meteorology is relative to Greenwich then set this 
                             !! to false. If the meteorology is all in local time set it to true. To
                             !! determine which, look at your shortwave radiation. As you move through time 
                             !! do you see the sun move across longitudes (allLocalTime -> .false.) or across 
                             !! latitudes (allLocalTime -> .true.). It seems reanalysis (e.g. CRU-based or MERRA)
                             !! will generally be false while climate model outputs (e.g. those from ISIMIP3 or 
                             !! GSWP3) are generally true. This won't be used for site-level simulations 
                             !! if your met will be coming into the model on a 30-min timestep.

    ! Meteorological forcing files. 
    ! For site-level simulations as long as you provide meteorology on the same timestep as the model physics (typically 
    ! 30 minutes) your meteorology will be used as is. 
      metFileFss = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_sw.nc',       !< location of the incoming total shortwave radiation meteorology file
      metFilefracFsf = '',        !< location of the incoming diffuse fraction of total shortwave radiation meteorology file (if none set empty like '')
      metFileFdl = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_lw.nc',       !< location of the incoming longwave radiation meteorology file
      metFilePre = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_pr.nc',         !< location of the precipitation meteorology file
      metFileSnow = '',        !< location of the snow meteorology file (if none set empty like '')
      metFileTa = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_ta.nc',          !< location of the air temperature meteorology file
      metFileQa = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_qa.nc',         !< location of the specific humidity meteorology file
      metFileUv = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_wi.nc',         !< location of the wind speed meteorology file
      metFilePres = '/work_zone/classic_tmp/inputFiles/meteorology/Juvenile/metVar_ap.nc',       !< location of the atmospheric pressure meteorology file

! Initialization and restart files
    init_file = '/work_zone/run_tmp/Juvenile/Juvenile_init.nc' ,     !< location of the model initialization file
    rs_file_to_overwrite = '/work_zone/run_tmp/Juvenile/rsfile.nc' ,      ! location of the existing netcdf file that will be 
                                              ! **overwritten** for the restart file typically here you
                                              !  will just copy the init_file and rename it so it can be
                                              ! overwritten.
! Namelist of model parameters
    runparams_file = '/work_zone/run_tmp/Juvenile/model_params.nml' ,    ! location of the namelist file containing the model parameters

! CTEM (biogeochemistry) switches:
 ctem_on = .true. ,     ! Set this to true for using ctem simulated dynamic lai and canopy mass, else class simulated specified
                        ! lai and canopy mass are used. With this switch on, all the main ctem subroutines are run.
    spinfast = 10 ,      ! Set this to a higher number up to 10 to spin up soil carbon pool faster. Set to 1 for final
                        ! round of spin up and transient runs. Setting to 1 also causes the calling of the balcar subroutine.
                        ! Balcar checks for conservation of carbon.

    ! Peatland switch:
    doPeatOutputs = .false. ,    ! This signals that peat specific output files should be made. This DOES NOT control the 
                                ! model calculations. These only occur if a tile has ipeatland > 0! So if you want to 
                                ! run with peatlands, you need to set up the model init file for them, and then turn this on.
    useStaticPeatDep = .true. , ! Only used if ipeatland is 1 or 2 for any gridcell. Ignored if no peatlands in simulation.
                                ! True will keep the peat depth set to the sdep value. This can be used for spinup to ensure
                                ! that the disequilibrium between the peatland and the driving climate, time period 
                                ! since the peatland initiation, or sub-grid heterogeneity impacts don't adversely impact the ability 
                                ! of the model to spinup. This should be set to .false. for transient simulations. Keep in mind that you  
                                ! need to do a difference between a run with transient climate and one without to determine the impact of 
                                ! the model responding to the newly dynamic peat depth.
                                
    ! Model tracer 
        useTracer = 'None' ,! Options:
                            ! useTracer -> 'None', the tracer code is not used. 
                            ! useTracer -> 'Simple' turns on a simple tracer that tracks pools and fluxes. The simple tracer then requires that the 
                            !               tracer values in the init_file and the tracerCO2file are set to meaningful values for the experiment being run.                         
                            ! useTracer -> '14C' means the tracer is 14C and will then call a 14C decay scheme. 
                            ! useTracer -> '13C' [Not implemented yet] means the tracer is 13C and will then call a 13C fractionation scheme. 
        tracerCO2file = ''  ! Tracer CO2 file, this file needs to be 
                            ! correctly chosen for the useTracer option. It uses the transientCO2 and fixedYearCO2 switches to determine how it is read in.
        tracertimeAdjust = 0 ,  ! This adjusts the read in year for the tracer. Default is 0.
                                ! E.g. if you start your run in year 1900 for readMetStartYear
                                ! and tracertimeAdjust -> -1000 then it will read in year 900 from the tracerCO2file. 

    !CO2 switches:
        transientCO2 = .false. , ! Read in time varying CO2 concentration from CO2File or if set to false then use only the year of fixedYearCO2 value
        CO2File = '/work_zone/classic_tmp/inputFiles/CO2/TRENDY_CO2_1700_2024.nc' ,
        fixedYearCO2 = 1750 ,   ! If transientCO2 -> .true., this is ignored.
        co2timeAdjust = 0 ,     ! This adjusts the read in year for the CO2. It only works if transientCO2 is true. Default is 0.
                                ! E.g. if you start your run in year 1900 for readMetStartYear
                                ! and co2timeAdjust -> -500 then it will read in year 1400 from the CO2file.

    !CH4 switches:
    
        doMethane = .false., ! If set to .true., a methane file should be specified below.

          ! Read in of atmospheric methane concentration for soil CH4 uptake. 
          transientCH4 = .false. , ! Read in time varying CH4 concentration from CH4File or if set to false then use only the year of fixedYearCH4 value
          CH4File = '/space/hall5/sitestore/eccc/crd/ccrp/users/scrd530/classic_inputs/CH4/CH4_1700_2020_for_GCP_2021.nc',
          fixedYearCH4 = 1700 ,   !If transientCH4 -> .true., this is ignored.

          ! Wetland switches:
              ! If you wish to read in and use observed wetland fractions, there are two options. If you wish time
              ! evolving wetland fractions set transientOBSWETF to true and give a OBSWETFFile. If you wish to use
              ! a single year of that file set transientOBSWETF to false, give a OBSWETFFile, and set fixedYearOBSWETF
              ! to some valid year. If you wish to use only dynamically determined wetland fractions set transientOBSWETF
              ! to false and set fixedYearOBSWETF to -9999. The slope fractions in the init_file will then be used to
              ! dynamically determine wetland extent.
              transientOBSWETF = .false. ,  ! use observed wetland fraction time series, otherwise use fixedYearOBSWETF
              OBSWETFFile = '/space/hall5/sitestore/eccc/crd/ccrp/users/scrd530/classic_inputs/wetland/global/T63/gcp-ch4_wetlands_1838-2017_t63_final_daily.nc', ! Location of the netcdf file containing observed wetland fraction
              fixedYearOBSWETF = -9999 ,    ! set the year to use for observed wetland fraction if transientOBSWETF is false.

    !Fire switches

        ! This switch controls the prognostic fire scheme (src/disturb.f90), CLASSIC can also do prescribed fire, see `prescribedFire` in the dynamic
        ! tiling switches below.

        dofire = .false. ,               ! If true the fire disturbance parameterization is turned on.

            fireSubmodule = 'Default',   ! This selects the fire submodule. The options here are:
                                         ! 'Default' - this is the present global fire model (e.g. Arora and Melton 2018 Nat. Commm.)
                                         ! 'FWI' - Canada-specific fire weather index based fire model. Presently NOT set up for global
                                         ! runs. 

                iniFWI = .true. ,        ! Only used if fireSubmodule is set to 'FWI'. Set this to true if you want to use
                                         ! the FWI values from the init (.false. resets them to the default values)

            transientPOPD = .false. ,    ! Read in time varying population density from POPDFile or if set to false then use only the year of fixedYearPOPD.
            POPDFile = '/space/hall5/sitestore/eccc/crd/ccrp/users/scrd530/classic_inputs/population/global/T63/popd_trendy_v14_1700_2025_t63.nc' ,
            fixedYearPOPD = 1700 ,      ! If transientPOPD -> .true., this is ignored.

            ! Set lightning behaviour. Note that lightning is daily. If you are using met with leap days the lightning file needs to have leap days in it.
            ! If you are cycling the met, you will need to set transientLGHT to false AND give the fixedYearLGHT a year with leap days in it.
            transientLGHT = .false.      ! Use lightning strike time series, otherwise use fixedYearLGHT
            LGHTFile = '/space/hall5/sitestore/eccc/crd/ccrp/users/scrd530/classic_inputs/lightning/global/T63/lightning_WGLC_timeseries_1700_2024_random_T63_scaled_chunked.nc' , ! Location of the netcdf file containing lightning strike values
            fixedYearLGHT = 2010 ,       ! Set the year to use for lightning strikes if transientLGHT is false.

    ! Turbation and per-layer litter fire-related switches:
    turbationON = .true. ,     ! Switch to use cryo-/bio-turbation (note that several model parameters need to be changed if this switch is turned)
    perlayerfireON = .false. , ! This switch makes it so fire only burns the top layer (note the model parameters may need to be changed after you turn on this switch)

    ! Competition switches:
        PFTCompetition = .false. ,      ! If true, competition between PFTs for space on a grid cell is implemented
            inibioclim = .false. ,      ! Set this to true if competition between pfts is to be implemented and you have the mean climate values
                                        ! in the init netcdf file.
            start_bare = .false.,       ! Set this to true if competition is true, and if you wish to start from bare ground. If this is set to false, the
                                        ! init netcdf file info will be used to set up the run. NOTE: This still keeps the crop fractions
                                        ! (while setting all pools to zero)

    ! Land Use switches:
        ! If you wish to use your own PFT fractional covers (specified in the init_file), set fixedYearLUC to -9999, otherwise set it
        ! to the year of land cover you want to use. If you wish to have transient land cover changes, set
        ! lnduseon to true, it will update the fractional coverages from LUCFile. When lnduseon is false it is
        ! not updated beyond the initial read in of landcover for fixedYearLUC, or if -9999 then the LUCFile is
        ! not used at all.
        lnduseon = .false. ,
        LUCFile = '/space/hall5/sitestore/eccc/crd/ccrp/users/scrd530/classic_inputs/landcover/global/T63/ulg_trendy2025_luh-gcb2025_landcover_t63_1700_2024_9pft_june_2025_rescaled.nc' 
        fixedYearLUC = -9999 ,

    ! Nitrogen cycle switches:
        Ncycle_on = .false. ,  !< set this to true to run all the subroutines associated with nitrogen cycle.

        NcycleConnect_on = .true.,  !< When set false and Ncycle_on is true, N limitation will not impact Vcmax. This 
                                    ! switch can be used to ensure the model doesn't get stuck in a low N state during 
                                    ! spinup. To do that, you can spinup with NcycleConnect_on = false until the C pools are 
                                    ! equilibrated, then set it to true and continue spinup until both the C and N pools
                                    ! are equilibrated.
  
       ! Fertilizer:
          fertilizeron = .false. ,
          transientFER = .false. ,  ! Read in time varying N fertilizer from FERFile or if set to false then use only the year of fixedYearFER.
          FERFile = '/work_zone/classic_tmp/inputFiles/Ncycle/nfer_TRENDYv13_t63.nc',
          fixedYearFER = 1700 ,
       
       ! Deposition:
          depositionon = .false. ,
          transientDEP = .false. ,  ! Read in time varying N deposition from DEPFile or if set to false then use only the year of fixedYearDEP.
          DEPFile = '/work_zone/classic_tmp/inputFiles/Ncycle/Ndep/Juvenile/Juvenile_Deposition.nc',
          fixedYearDEP = 2016 ,
    
    !Dynamic tiling switches:
        dynamicTilingOn = .false. !< set this to true to have access to dynamic tiling and tiling enabled disturbance

        !tile age switches (this can be used with or without dynamic tiling, but makes more sense when dynamic tiling is on):
            trackTileAge = .false. !< if true the model will track tile age as it runs
            tileAgeReset = .false. !< resets the age of the tile to zero at the start of this run
        
        !timber harvest forcing switches (can be used with or without dynamic tiling):
            timberHarvest = .false. , !< Simulate timber harvest
            timberHarvestFile = "", !< Path to a file detailing the annual gridcell fraction harvested
            
        !fire forcing switches (can be used with or without dynamic tiling):
            ! If using prescribed fire, set dofire to false above.
            prescribedFire = .false. , !< Simulate fire with a forcing
            prescribedFireFile = "", !< Path to a file detailing the annual or monthly gridcell fraction burned
            prescribedFireTimestep = 'Annual',   ! This selects the time step of the prescribedFireFile. The options here are: 'Annual' or 'Monthly'.
                                                 ! Presently the 'Monthly' option is NOT set up for dynamicTilingOn. Use an 'Annual' prescribed fire file in that case.

! Physics switches:

    IDISP = 1 ,   !  This switch controls the calculation of the vegetation displacement height. In most
                  ! atmospheric models a “terrain-following” coordinate system is used, in which the vegetation
                  ! displacement height is considered to be part of the “terrain”, and is therefore neglected. 
                  ! For such applications IDISP is set to 0. For studies making use of field data, it should be set to 1.
    IZREF = 1 ,   ! This switch indicates where the bottom of the atmosphere is conceptually located. In
                  ! most atmospheric models the bottom is assumed to lie at the local surface roughness length, 
                  ! i.e. where the horizontal wind speed is zero; for such simulations IZREF is set to 2. For 
                  ! all other cases, including sites using field data, it should be set to 1.
    ZRFH = 32 ,  ! The reference heights at which the energy variables (air temperature and specific humidity) are provided. When 
                  ! using field data this is the measurement height. 
    ZRFM = 28 ,  ! The reference heights at which the momentum variables (wind speed) are provided. When using field data this is the measurement height. 
    ZBLD = 50.0,  ! The atmospheric blending height.  Technically this variable depends on the length scale of the
                  ! patches of roughness elements on the land surface, but this is difficult to ascertain.  Usually it is assigned a value of 50 m.
    ISLFD = 1 ,   ! This switch indicates which surface layer flux stability
                  ! correction and screen-level diagnostic subroutines are to be
                  ! used. If ISLFD->0, the CCCma stability correction subroutine
                  ! DRCOEF is used with simple similarity-based screen-level
                  ! diagnostic calculations. If ISLFD->1, DRCOEF is used for the
                  ! stability corrections and the RPN subroutine SLDIAG is used
                  ! for the screen level diagnostic calculations. If ISLFD->2, the
                  ! RPN stability correction subroutine FLXSURFZ is used with the
                  ! companion RPN subroutine DIASURFZ for the screen level
                  ! diagnostics. (When running CLASS coupled to an atmospheric 
                  ! model with staggered vertical thermodynamic and momentum
                  ! levels, ISLFD must be set to 2, since FLXSURFZ allows inputs
                  ! on staggered levels and DRCOEF does not.)
    IPCP = 2 ,    ! If a metFileSnow is supplied, this option is *IGNORED*. If ipcp->1, the rainfall-snowfall cutoff is taken 
                  ! to lie at 0 C. If ipcp->2, a linear partitioning of precipitation between rainfall and snowfall is done 
                  ! between 0 C and 2 C. if ipcp->3, rainfall and snowfall are partitioned according to
                  ! a polynomial curve between 0 C and 6 C. 
    IWF = 0 ,     ! If iwf->0, only overland flow and baseflow are modelled, and the ground surface slope is not modelled. if iwf->n (0<n<4),
                  ! the watflood calculations of overland flow and interflow are performed; interflow is drawn from the top n soil layers.

    ! Fractional snow cover switch:
    fracSnowParam = 'Default', ! Two options: 'Default' uses the original default CLASS scheme, SCF->SD/0.1.
                               ! while 'SL12' uses the SL12 scheme (Swenson and Lawrence, 2012). 

    ! Albedo scheme switches:
    snoAlbedoParam = '2-band' , ! Options: '2-band' - the original CLASS two-band snow albedo algorithms OR 
                                ! '4-band' - CanESM's four-band routines. '4-band' also then requires the 
                                ! alb4BandParamsFile to be specified and optionally, one can run with black carbon deposition 
                                ! considered by setting blackCdepon to true.
                                ! At present, the four band algorithm is NOT recommended to be used offline.
        
        alb4BandParamsFile = '/space/hall5/sitestore/eccc/crd/ccrp/users/scrd530/classic_inputs/albedo_lookup/classic4BandAlbedoLookUpFile.nc',  
            ! Look up table file for the '4-band' albedo parameterization (if snoAlbedoParam -> '2-band' set empty like '')
    
        ! Black carbon deposition
            blackCdepon = .true. ,  ! If true, include consideration of black carbon deposition in snow processes (4-band scheme)
            blackCtransientDep = .false. ,  ! Read in time varying black carbon deposition from DEPblackCarbonFileFile or if set to false then use only the year of fixedYearBCDep.
            blackCarbonFile = '', ! Black carbon deposition flux file. If snoAlbedoParam -> '2-band', this can be left empty.
            fixedYearBCDep = 1850 ,  ! Set the year to use for black carbon deposition flux if blackCtransientDep is false.

    ! Saturated soil hydraulic conductivity (Ksat) scalar for reduction with depth
        KsatScalaron = .false. ,  ! If true, include exponential distribution scalar for saturated hydraulic conductivity (GRKSAT) in soilProperties.f90. NOTE this parameterization           
                                 ! requires drainage index (DRN), a variable in the initial conditions file, to be set to 1.0. 

    ! Specify a uniform maximum soil permeable depth
        specifiedSoilPermDepth = -9999 ! If this is set to a positive real number, the code will OVERWRITE the SDEP (soil permeable depth)
                                        ! read in from the initial conditions file. The SDEP will then be the minimum of the value read in and 
                                        ! the specifiedSoilPermDepth value (so specifiedSoilPermDepth becomes the maximum possible). Do not 
                                        ! confuse this with the maximum ground depth (both permeable soil and bedrock) - that is determined by
                                        ! the number and thickness of ground layers the run is set up with! If you wish to use the SDEP value 
                                        ! in the initial conditions file, set this to -9999. (or any negative real number).

    ! Switch for partitioning into canopy evaporation (Ec) and transpiration (T)
        EcTResistancePart = .true. ,  ! Switch determining whether to use the leaf boundary layer and stomatal resistances to determine the partitioning 
                                      ! into canopy evaporation and transpiration or the previous standard approach, where transpiration only occurs, when
                                      ! there is no intercepted water on the canopy; set to false for original calculation in canopyWaterUpdate.f90

 ! Iteration scheme
    ! ITC, ITCG and ITG are switches to choose the iteration scheme to be used in calculating the canopy or ground surface temperature
    ! respectively.  if the switch is set to 1, a bisection method is used; if to 2, the newton-raphson method is used. (Note: Recently
    ! problems have been discovered with the Newton-Raphson scheme, involving instabilities and occasional failure to converge, so 
    ! currently users are advised not to select this option.)

    ITC = 1 ,   ! Canopy
    ITCG = 1 ,  ! Ground under canopy
    ITG = 1 ,   ! Ground

 ! User-supplied values:
    ! if ipai, ihgt, ialc, ials and ialg are zero, the values of plant area index, vegetation height, canopy albedo, snow albedo
    ! and soil albedo respectively calculated by class are used. if any of these switches is set to 1, the value of the
    ! corresponding parameter calculated by class is overridden by a user-supplied input value.
    IPAI = 0 ,  ! Plant area index
    IHGT = 0 ,  ! Vegetation height
    IALC = 0 ,  ! Canopy albedo
    IALS = 0 ,  ! Snow albedo
    IALG = 0 ,  ! Soil albedo

! Output options:

    output_directory = '/work_zone/run_tmp/outputFiles/Juvenile/' ,        !< Directory where the output netcdfs will be placed
    xmlFile = '/work_zone/classic_tmp/configurationFiles/outputVariableDescriptors.xml' ,  !< location of the xml file that outlines the possible netcdf output files

    doperpftoutput = .false. ,   ! Switch for making extra output files that are at the per PFT level 
    dopertileoutput = .false. ,  ! Switch for making extra output files that are at the per tile level [half-hourly per-tile output not implemented yet]
    
    dohhoutput = .false. ,      ! Switch for making half-hourly output files [half-hourly per-tile output not implemented yet]
    JHHSTD = 166 ,              ! day of the year to start writing the half-hourly output
    JHHENDD = 185 ,             ! day of the year to stop writing the half-hourly output
    JHHSTY = 1901 ,             ! simulation year (iyear) to start writing the half-hourly output
                                ! **If you're looping through the met data, this must equal runStartYear**
                                ! **If leap -> true, this must equal runStartYear (whether you're looping or not)**
    JHHENDY = 1901 ,            ! simulation year (iyear) to stop writing the half-hourly output

    dodayoutput = .false. ,     ! Switch for making daily output files 
    JDSTD = 1 ,                 ! day of the year to start writing the daily output
    JDENDD = 366 ,              ! day of the year to stop writing the daily output (NOTE: if you have leap -> .true.,
                                ! and you want all days written, this needs to be 366 to get the final day of the year written!)
    JDSTY = 2016 ,              ! simulation year (iyear) to start writing the daily output
                                ! **If you're looping through the met data, this must equal runStartYear**
                                ! **If leap -> true, this must equal runStartYear (whether you're looping or not)**
    JDENDY = 2024 ,             ! simulation year (iyear) to stop writing the daily output

    domonthoutput = .false. ,    ! Switch for making monthly output files 
    JMOSTY = 1860 ,             ! Year to start writing out the monthly output files. 
                                ! **If you're looping through the met data, this must equal runStartYear**
    
    doAnnualOutput = .true. ,   ! Switch for making annual output files 

    doChecksums = .false.,      ! checksums can be generated if you wish to ensure you are making no
                                ! functional changes to the model results. See the CLASSIC documentation and
                                ! the regression_testing tool.
    
    Comment = ' Mon Jul 20 15:51:21 KST 2026 run - with Ncycle .false. - on_HPC being false'          ! Comment about the run that will be written to the output netcdfs

 /

This area can be used for comments about the run.
